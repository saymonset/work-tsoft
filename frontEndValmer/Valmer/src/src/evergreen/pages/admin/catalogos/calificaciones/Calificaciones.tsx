import {DataCatCalif} from "../../../../../utils";
import {ShowEditCatalog} from "../../../../../shared";

export const Calificaciones = () => {
    return (
        <ShowEditCatalog titleName="Catálogo Calificaciones" DataCatalog={DataCatCalif}/>
    )
}